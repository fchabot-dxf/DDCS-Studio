# Community macros — DDCS V4.1

Drop community-authored `.nc` / macro files for the **V4.1** here (zips fine). They form the
**COMMUNITY-ATTESTED** evidence tier: below the factory corpus (`../firmware/`, `../system-backup/`),
above pure inference. Factory V4.1 macros already demonstrate COS/SIN/ROUND/FIX — community files are
most valuable where the factory corpus is silent: SQRT, ATAN, position-register reads.

One line per file below, best-effort:

| file | source (forum/person/link) | controller + firmware it ran on | author says it works? |
|---|---|---|---|
