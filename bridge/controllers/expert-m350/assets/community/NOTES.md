# Community macros — Expert M350 (DDCS)

Drop community-authored `.nc` / macro files for the **Expert M350** here (zips fine). They form the
**COMMUNITY-ATTESTED** evidence tier: below the factory corpus (`../capture/`), above pure inference.
A community macro that its author ran on a real machine is field evidence that the parser accepts its
syntax and functions — which is exactly what the open questions need (SQRT/ATAN appear in NO factory
dump on any DDCS controller; `#790/#791` likewise).

One line per file below, best-effort — provenance is what upgrades a hit from "found on the internet"
to "attested":

| file | source (forum/person/link) | controller + firmware it ran on | author says it works? |
|---|---|---|---|
| (example) spiral-pocket.nc | ddcs users FB group | Expert M350 fw 2024-xx | yes, video |
