# Community macros — DM500 (DDCS V3)

Drop community-authored `.nc` / macro files for the **DM500 / V3** here (zips fine). They form the
**COMMUNITY-ATTESTED** evidence tier: below the factory corpus (`../../install/`), above pure
inference. The factory DM500 library demonstrates COS/SIN/ABS and the `#864-#866` machine-position
family — community files are most valuable where it is silent: SQRT, ATAN, ROUND, WCS-position reads.

One line per file below, best-effort:

| file | source (forum/person/link) | controller + firmware it ran on | author says it works? |
|---|---|---|---|
