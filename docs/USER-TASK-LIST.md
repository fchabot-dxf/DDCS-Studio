make view cube clicking more robust
drill: drill and bore should be seperate wizard, and pecking should be an increm,ental stepdown, now it looks like its just doing X times the same depth.
in block tab, desktop mode( wide) make preview seperation draggable with a handle, same as mobile
add a way for users to define default for wizards, or even to save templates, in the controller profile file maybe? or in a seperate file?
in wizards and editor and block tab, id like to be able to click in the code to put my cursor, and see in the preview where it lands, maybe position the tool in preview to match my cursor in code

edge wizard starty position are wrong, you can use corner and middle to reference for start position

