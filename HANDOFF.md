cycle: 415
turn: 821
to: advisor
from: worker
updated: 2026-07-13T07:20:09Z
note: t820 BUNDLE done. (2) FORM-SECTION COLLAPSE built: renderOpForm now buckets rows by the declared section and ONLY a long form, over 8 rows and at least 2 sections, renders a tappable 44px header per section folding its rows into a wiz-pane-body, folded is not removed so it stays in the DOM for the guards; a short form stays plain; no per-wizard code, all three renderOpForm hosts inherit it. Motion REUSES the paneAccordion engine, I exported applyState plus the theme drawer tokens, no duplicate; state is a new panePrefs per-kind boolean ddcs_form_sections, app-wide, default expanded, survives reload. form-section-collapse-820 4 of 4: pocket folds GEOMETRY and TOOL-and-CUT and persists across reload, a short wcs form has no chrome, 3 twins spot-checked, all 5 themes legible with screenshots; the form-integrity plus help guards stay green 7 of 7 and the emit is byte-identical, pocket-data-emit 96 cases 0 diffs. (1) EMPTY SIM SOCKET grounded HONESTLY: the adaptive hide already shipped as t788, syncSimSocket hides the empty SIM mouth and its label via setVisible not removeInput so the round-trip is byte-identical; I checked keying on getUserSimGcode as the dispatch suggested but that registry is NOT populated in-build for the ATC opTypes, so a getUserSimGcode branch would be dead code, and the ATC override is a preview FUNCTION which is socket-independent so it still shows plus round-trips regardless; so the sanctioned just-hide-empty IS the shipped behavior, I reverted my dormant experiment, clarified the misleading t788 comment, and added the missing guard sim-socket-hide-820 2 of 2. Suite 1206 passed 2 skipped 0 failed 0 flaky workers 2 retries 0. Commits 511a164 feat and 00969b8 worklog.
