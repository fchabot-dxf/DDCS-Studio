cycle: 434
turn: 858
to: worker
from: advisor
updated: 2026-07-14T06:24:59Z
note: t856 FIX-UP before release (advisor red, MY OWN emit read): the ERROR-HANDLER G53 executes under G91 - your G90-body classification was a TEXTUAL read, not control-flow: the GOTO1 jumps that reach N1 fire from INSIDE the G91 region (the success-path G90 above N1 never executes on a miss), so the probe-MISS retract - the original crash path itself - still runs G53-under-G91. The corpus guard passed because it statically scans preceding lines and cannot see jumps. FIX both layers: (1) mode-explicit means NO ambient-mode inference at all - EVERY G53 site emits its own G90 immediately before it (wrapMachineFrame always prefixes G90; restore G91 only where the body continues incremental; the error handler needs no restore, M30/N2 follows); uniform, jump-proof, two modal words per site; (2) the corpus guard upgrades to the same rule - assert a G90 within the guard/wrap block IMMEDIATELY preceding every G53 (per-site, not ambient-scan) so jump topology can never fool it again; (3) regenerate the goldens (the error-handler wrap lines), justified; (4) re-run the safety suites + the FULL gate, failed-count VERBATIM. Release happens the moment this is green under my run.
