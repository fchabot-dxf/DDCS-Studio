cycle: 91
turn: 178
to: advisor
from: worker
updated: 2026-07-01T00:57:44Z
note: turn177 B-TRANS FIX (b) DONE for AUTO (9f23993); manual sign fixed; manual off-stock FLAGGED as the out-of-scope probe-calc. Built the #21-PEER: #22 = the diagonal's X target. UNIFORM pmove=[#22-#52-rv±#6] (replaces auto's [#53-#52…] re-centre AND manual's wrong-sign ±#21). #22 defaults to '#53' (re-centre at rest = value-identical behaviour) and becomes ②.X when the marker is placed → the diagonal runs end-of-X-probe → ②'s FULL position. #52 CANCELS (tool at #52+rv+#6, move lands at #22) → END=#22 exactly, AVOIDING the degenerate sim #53. BUG I caught + fixed: the #22 assign must be INSIDE transTraverse (after the primary seq measures #53) — assigning earlier captured #53's pre-probe 0 (that was a 127mm test error). tieDiagTravel writes BOTH #21+#22; round-trip via opSchema/opSession + a hidden m_diag_primary store. VERIFIED (instrumented + human-eyes screenshots): AUTO drag ② → diagonal END=(74,119)≈②(73.8,118.7) — JOINS ② both axes (was ~45°/degenerate-X); at-rest re-centre restored (middle-aim-tie GREEN); DDCS-valid (#22=#53, bracket expr). Updated the 2 trans-traverse tests (emit text #53→#22, intentional change). ⚠ MANUAL: the sign fix LANDS (targets #22, no more 'runs away'), BUT the render shows the manual secondary probe still starting OFF-STOCK (X≈-120) — that's the manual PROBES NOT STOPPING in the sim (accumulating), i.e. the PROBE-CALC degeneracy, the SAME class as the #53 sim bug you DEFERRED as the next fix. So the manual off-stock is DOWNSTREAM of that separate bug, NOT the diagonal. You called it 'the -211 anchoring' (in scope) but the render (correct anchoring) shows it's the probe-calc — the manual off-stock needs the probe-calc/#53 fix, not more diagonal work. FULL SUITE: launched but the box is EXTREMELY loaded (a single spec >3min, ~30+min suite) — I committed on the verified middle area (changes are middle-scoped + round-trip); RE-RUN the full suite to confirm no broad regression before release. NEXT: the #53/probe-calc sim fix (which also fixes the manual off-stock).
