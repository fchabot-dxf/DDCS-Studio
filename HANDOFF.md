cycle: 563
turn: 1114
to: advisor
from: worker
updated: 2026-07-24T03:59:19Z
note: GATE-BACK -- S6 polish: I grounded all 3 items and item (3) the knob-removal PREMISE IS DISPROVEN empirically; no code ships (my one attempt was reverted). The details:

ITEM (3) REMOVE/HIDE THE VESTIGIAL KNOB -- PREMISE INCORRECT, DO NOT REMOVE/HIDE. I VERIFIED BEFORE REMOVING (you mandated it). Two findings. (A) PROBE of the 25 registered wizards: ZERO carry _expose, so the pill migration IS complete for the stored DATA (nothing to strand). BUT (B) the knob is NOT vestigial -- it is a load-bearing, USER-REQUESTED, heavily-tested subsystem serving TWO roles via the SAME UI element (the EXPOSE checkbox): role 1 is LIVE param-CREATION for hand-built plus group authoring (collectAuthoring reads live EXPOSE then buildBindings; it writes _expose; deriveGroupDef reads stored _expose) tested by knob-persist, dev-mode, group-auto/edit/framing/canvas-knob; role 2 is the t391 PROVENANCE DISPLAY (the user asked for this: quote do knob, it helps me troubleshoot to use Blocks) where a data-op twins FORM-BOUND params show as PRE-TICKED knobs in Blocks (a binding IS a knob, same record, emit byte-identical) tested by blocks-knob-binding (opens user_corner_data, asserts dist/f_fast/... pre-ticked). ~10 specs total. PROOF the hide is unsafe: I built the NARROWEST possible hide -- gate augment knob rows to collectAuthoring SCAN set (preorderAtoms of the authoring body first), removing ONLY the dead twin knobs (a data-op body is user_root whose exec atoms live in mouths preorderAtoms never descends, the wrapForkAtSave NOTE at devMode line 480). It PASSED knob-persist plus dev-mode (hand-built DO-chain, scanned) but FAILED blocks-knob-binding (dist.EXPOSE_VALUE undefined) -- the dead twin knobs ARE the t391 provenance. So even the narrowest hide breaks a user-requested feature. REVERTED (devMode.js byte-identical to HEAD). RECOMMENDATION: do NOT remove/hide the knob. The t1112 remove-the-checkbox request CONFLICTS with the t391 show-bound-params-as-knobs request -- same element. This is a USER fork to re-confirm with the user: (a) KEEP it (it is not vestigial); or (b) if the goal is purely a VISUAL declutter of UNTICKED create-affordances while KEEPING the pre-ticked provenance plus the create path, that is a DISTINCT non-trivial ask -- have the user specify it and I will scope it. Per confirm-premise-before-regression-blocker plus confirm-the-referent-before-dropping I did not proceed.

ITEM (1) COMPACT BLOCK LAYOUT -- GATE, needs a mechanism decision. Grounded: the vendored Blockly has NO Field.setVisible; jsonDef renders ALL fields INLINE on message0; ddcs_dynfields hides via getInput(name).setVisible which only works for VALUE-SOCKET inputs (Number/Region/Boolean) -- a NO-OP for inline text fields. So array/formfield dynamic works ONLY because their toggle-able fields are numeric SOCKETS; cam_field/param_field toggle-able fields (baked/units/nmin/nmax/options) are inline text so no existing mechanism hides them. The fix needs EITHER (a) jsonDef renders each toggle-able field on its OWN named dummy input for a flagged block then ddcs_dynfields hides per mode/widget (a one-field-per-row layout change; round-trip/emit unchanged since fields serialize by NAME); OR (b) a re-render-on-change extension that rebuilds fields with fieldsFor(mode) on a mode change (mutator-lite). Both DISPLAY-ONLY plus byte-neutral. I RECOMMEND (a) -- reuses proven ddcs_dynfields, change stays in jsonDef. Provisional until you pick; it is real new machinery so I gated it rather than balloon the polish slice.

ITEM (2) LITERAL-TWIN NO-PILL SAVE FIX -- GATE (secondary, entangled, you pre-authorized). Untouched.

NET: the measured gate PREVENTED breaking a user-requested feature (t391) plus ~10 specs. Baseline green, devMode.js unchanged, process tree clean, no release. Docs commit 5a940ba (WORK-LOG only). Saved a memory inline-knob-not-vestigial so it is not re-litigated. Over to you: re-confirm the item-3 premise with the user, pick the item-1 mechanism (a vs b), and direct S6b.
